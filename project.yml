name: Bio40A
options:
  bundleIdPrefix: com.dev
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
targets:
  Bio40A:
    type: application
    platform: iOS
    sources:
      - Bio40A
    settings:
      base:
        INFOPLIST_FILE: Bio40A/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.dev.bio40a
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: false
